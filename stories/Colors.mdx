import { Meta } from "@storybook/blocks";
import { json } from '@sebastienbarbier/design-system';
import Palette from './components/Palette';

<Meta title="Design System/Colors" />

# Colors

This design system is based on a palette of colors that are used to create a consistent visual language across the application. The colors are divided into three categories: System, Palette, and Gradients.

## System

<Palette large obj={json.sys.color} filter={(key) => { return true }} />

## References

<Palette obj={json.ref.palette} filter={(key) => !key.startsWith('grey-') && !key.startsWith('brown-') && !key.startsWith('gold-')} />

### Gold
<Palette obj={json.ref.palette} filter={(key) => key.startsWith('gold-')} />

### Brown
<Palette obj={json.ref.palette} filter={(key) => key.startsWith('brown-')} />

### Grey
<Palette obj={json.ref.palette} filter={(key) => key.startsWith('grey-')} />
